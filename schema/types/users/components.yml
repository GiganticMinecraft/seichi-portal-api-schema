components:
  schemas:
    uuid:
      description: Minecraftプレイヤーに紐づくUUID
      type: string
      format: uuid
    name:
      description: Minecraftプレイヤー名
      type: string
    role:
      description: ユーザー権限
      type: string
      enum:
        - Administrator
        - StandardUser
