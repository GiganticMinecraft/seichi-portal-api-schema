params:
  question_id:
    in: path
    required: true
    schema:
      $ref: '#/schemas/question_id'
schemas:
  question_dynamic_props:
    type: object
    properties:
      title:
        type: string
        example: 質問1
      description:
        type: string
        example: 質問1の説明
      question_type:
        type: string
        example: TEXT
        enum:
          - TEXT
          - PULLDOWN
          - CHECKBOX
      choices:
        type: array
        uniqueItems: true
        items:
          type: string
          example: '選択肢'
    required:
      - title
      - question_type
  question_id:
    type: integer
    example: 0
  question:
    type: object
    allOf:
      - properties:
          id:
            $ref: '#/schemas/question_id'
      - $ref: '#/schemas/question_dynamic_props'
  questions:
    type: array
    uniqueItems: true
    items:
      $ref: '#/schemas/question'
